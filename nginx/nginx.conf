events {}

http {
  server {
    listen 80;

    location / {
      root /usr/share/nginx/html;
      index index.html;
    }

    location /admin {
      root /usr/share/nginx/html;
      index admin.html;
    }

    location /api/ {
      proxy_pass http://catalog:3000/;
    }
  }
}
